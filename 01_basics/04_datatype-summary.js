// primitive datatype : string, boolean, undefine, bigint ,number,null,symble
// Nonprimitive (Refrance): array ,object, function

const score=100
const scoreValue=101.0

const isLoggedIn=true
const outerTemp=null

let userEmail= 'Shahjankhan089gmailcom';

const id= Symbol('123')
const anotherId=Symbol('123')
console.log(id === anotherId);

const heros=['Yaya rana ','saktiman'];

let myObj={
    name:'Shahjan khan',
    roll:101,
    age:24
}

let  myFunction =function(){
    console.log('Hello words');

}